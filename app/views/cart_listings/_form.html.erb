<%= form_with(model: cart_listing) do |form| %>
  <% if cart_listing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cart_listing.errors.count, "error") %> prohibited this cart_listing from being saved:</h2>

      <ul>
        <% cart_listing.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
