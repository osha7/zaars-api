<%= form_with(model: listing_tag) do |form| %>
  <% if listing_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(listing_tag.errors.count, "error") %> prohibited this listing_tag from being saved:</h2>

      <ul>
        <% listing_tag.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
