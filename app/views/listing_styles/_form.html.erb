<%= form_with(model: listing_style) do |form| %>
  <% if listing_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(listing_style.errors.count, "error") %> prohibited this listing_style from being saved:</h2>

      <ul>
        <% listing_style.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
